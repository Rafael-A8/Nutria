# M2.4.4d — Canonical Reviewed Resolution for the First Controlled Batch

## 1. Resultado

Foi criada e validada uma única resolução revisada canônica para o primeiro lote controlado:

```text
resources/catalog-import/review/legacy_config_nutrition_v1/reviewed-resolution-8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d.json
```

Resultado exato:

```text
schema:                          nutria.catalog-import-resolution/1
SHA-256:                         8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d
byte size:                       230948
review entries:                  106
resolved candidate identities:     3
unresolved candidate identities: 103
selected candidates:               3
selected valid candidates:         3
selected suspicious candidates:    0
selected invalid candidates:       0
included alias groups:              3
excluded alias groups:              2
unresolved selected alias groups:   0
```

Os três candidatos selecionados foram aceitos pelo `CatalogImportReviewEligibilityValidator` sem nenhuma razão de inelegibilidade. Os outros 103 permanecem integralmente iguais ao template unresolved, sem decisões e sem seleção.

Esta fase não criou atestação formal, apply plan, registro de banco ou evento de lifecycle. Nenhum contrato de produção ou teste foi alterado.

## 2. Inputs aprovados

### 2.1 Candidate manifest

```text
path: resources/catalog-import/approved/legacy_config_nutrition_v1/candidate-manifest-4e5e5c3c505fca1d613ef8c3dee6bd066cd28876a49cd1b47dd543d9b4996ee2.json
schema: nutria.catalog-import-manifest/1
SHA-256: 4e5e5c3c505fca1d613ef8c3dee6bd066cd28876a49cd1b47dd543d9b4996ee2
source SHA-256: 0d3987cc616b40e878731ecda0127a5b0a065f9557977b5b8f4ec0091d4ecc21
candidate count: 106
```

O checksum foi calculado independentemente antes e depois da geração. O arquivo permanece byte-idêntico à versão comprometida e sem diff contra `HEAD`.

### 2.2 Unresolved resolution template

```text
path: resources/catalog-import/review/legacy_config_nutrition_v1/resolution-template-b9c1d4ae30c70208bf57bea51e6a6824886e129ecda20afe632ea3f47d28889e.json
schema: nutria.catalog-import-resolution/1
SHA-256: b9c1d4ae30c70208bf57bea51e6a6824886e129ecda20afe632ea3f47d28889e
review entries: 106
resolved identities: 0
unresolved identities: 106
selected candidates: 0
approved apply-plan records: 0
```

O checksum foi calculado independentemente antes e depois da geração. O template permanece rastreado, byte-idêntico à versão comprometida e ao lado da nova resolução revisada.

## 3. Preflight fresco somente leitura

O comando M2.4.4a comprometido foi executado contra o manifesto aprovado, usando somente os destinos ignorados autorizados:

```text
storage/app/m244d-current-unresolved-template.json
storage/app/m244d-current-preflight.json
```

O template diagnóstico reproduziu exatamente o SHA-256 aprovado:

```text
b9c1d4ae30c70208bf57bea51e6a6824886e129ecda20afe632ea3f47d28889e
```

### 3.1 Contagens atuais do catálogo

| Entidade | Contagem |
|---|---:|
| Sources | 0 |
| References | 0 |
| Reference versions | 0 |
| Reference-version sources | 0 |
| Aliases | 0 |

### 3.2 Evidências e conflitos globais

| Evidência ou conflito | Total |
|---|---:|
| Stable-key matches | 0 |
| Public-UUID matches | 0 |
| Normalized canonical-name matches | 0 |
| Normalized alias matches | 0 |
| Total exact-match evidence | 0 |
| Stable-key conflicts | 0 |
| Public-UUID conflicts | 0 |
| Immutable-field conflicts | 0 |
| Total conflicts | 0 |

### 3.3 Evidência por candidato

| `source_record_key` | Exact stable key | Exact public UUID | Canonical match | Alias match | Immutable conflict | Existing reference candidate |
|---|---:|---:|---:|---:|---:|---:|
| `leite condensado` | 0 | 0 | 0 | 0 | 0 | none |
| `doce de leite` | 0 | 0 | 0 | 0 | 0 | none |
| `creme de leite` | 0 | 0 | 0 | 0 | 0 | none |

Para cada um dos três:

```text
possible_exact_catalog_matches: []
preflight_conflict_decisions: []
```

Nenhum candidato foi silenciosamente convertido para `existing_reference`.

### 3.4 SQL e efeitos colaterais

```text
read_only: true
SELECT query count: 7
write statements: 0
DDL statements: 0
locks: 0
```

Query kinds exatos:

```text
count_aliases
count_reference_version_sources
count_reference_versions
count_references
count_sources
exact_current_canonical_name_matches
exact_current_alias_matches
```

Não houve `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, `MERGE`, transaction usada como substituto de read-only, chamada de lifecycle ou supersession.

## 4. Procedimento canônico

Foi usado um script PHP temporário e ignorado em `storage/app`, exclusivamente para esta operação. Ele:

1. verificou os checksums dos dois inputs;
2. decodificou o template rastreado;
3. localizou somente os três `source_record_key` aprovados;
4. validou as UUIDs planejadas imutáveis antes de aplicar decisões;
5. modificou somente os campos editoriais aprovados;
6. recalculou o summary como `3/103/3/0`;
7. serializou com `CanonicalCatalogImportJson::serializeSemanticGraph()`;
8. escreveu bytes canônicos temporários sem formatação manual;
9. validou o arquivo com o loader e validators comprometidos;
10. avaliou a elegibilidade individual dos três selecionados.

Não foi usado um segundo serializador. O script foi removido após o uso. As duas cópias temporárias de geração também foram removidas após a comparação e preservação da cópia durável.

O schema não possui um campo editável separado `identity_resolution.status`. O estado resolvido é derivado pelo validator a partir das decisões completas e materializado no campo comprometido `summary.resolved_candidate_identities=3`. Nenhuma chave nova foi inventada.

## 5. Decisões canônicas compartilhadas

As três entradas selecionadas contêm:

```text
reference_target: new_reference
existing_reference_public_id: null
reference_visibility: global
owner_user_id: null
owner_user_id_decision: explicit_null
is_generic: true
version_locale: pt-BR
catalog_classification: food
preparation_decision.status: explicit_null
preparation_decision.preparation_key: null
selected_for_apply: true
```

`existing_reference_public_id`, `owner_user_id` e `preparation_key` já eram `null` no template; continuam explicitamente nulos, portanto seus bytes não aparecem como caminhos alterados. A decisão de owner e a decisão de preparação ficaram explícitas nos respectivos status.

Os issue codes originais, inclusive `source_untrusted` e os marcadores históricos de identidade unresolved do candidate manifest, permanecem como fatos imutáveis de provenance. Eles não foram reescritos; elegibilidade e summary registram a resolução editorial atual.

## 6. Decisões exatas por candidato

### 6.1 `leite condensado`

```text
stable_key: leite-condensado
planned_reference_uuid.authority: non_authoritative
planned_reference_uuid.public_id: 6e360841-51b8-5deb-a201-38debd0f03dc
calorie evidence: 313 kcal per 100 g
```

Nota editorial exata:

```text
First controlled batch: generic global leite condensado concept. Legacy source remains primary/untrusted. The value 313 kcal per 100 g is preserved without nutritional verification. The typo "leite condesado" remains provenance-only. Future version and alias records remain drafts.
```

Alias decisions, na ordem herdada:

| Normalized alias | Status | Kind | Fatos preservados |
|---|---|---|---|
| `leite condensado` | `include` | `common` | Display, raw variants e ordinal permanecem inalterados. |
| `leite condesado` | `exclude` | `null` | Typo permanece no documento como provenance/candidate fact. |

### 6.2 `doce de leite`

```text
stable_key: doce-de-leite
planned_reference_uuid.authority: non_authoritative
planned_reference_uuid.public_id: 0d3adf20-be25-5439-b89b-98e86e78c995
calorie evidence: 306 kcal per 100 g
```

Nota editorial exata:

```text
First controlled batch: generic global doce de leite concept. Legacy source remains primary/untrusted. The value 306 kcal per 100 g is preserved without nutritional verification. The form "doce leite" remains provenance-only. Future version and alias records remain drafts.
```

Alias decisions, na ordem herdada:

| Normalized alias | Status | Kind | Fatos preservados |
|---|---|---|---|
| `doce de leite` | `include` | `common` | Display, raw variants e ordinal permanecem inalterados. |
| `doce leite` | `exclude` | `null` | Forma excluída permanece no documento como provenance/candidate fact. |

### 6.3 `creme de leite`

```text
stable_key: creme-de-leite
planned_reference_uuid.authority: non_authoritative
planned_reference_uuid.public_id: 8e502950-ca1d-557d-8c26-c734b795f238
calorie evidence: 221 kcal per 100 g
```

Nota editorial exata:

```text
First controlled batch: generic global creme de leite concept without inferred fat-content, processing, or presentation subtype. Legacy source remains primary/untrusted. The value 221 kcal per 100 g is preserved without nutritional verification. Future version and alias records remain drafts.
```

Alias decision, na ordem herdada:

| Normalized alias | Status | Kind | Fatos preservados |
|---|---|---|---|
| `creme de leite` | `include` | `common` | Display, raw variants e ordinal permanecem inalterados. |

## 7. Elegibilidade exata

| Candidato | `eligible` | `reasons` | `selected_for_apply` |
|---|---:|---|---:|
| `creme de leite` | `true` | `[]` | `true` |
| `doce de leite` | `true` | `[]` | `true` |
| `leite condensado` | `true` | `[]` | `true` |

Ordem selecionada retornada pelo loader:

```text
creme de leite
doce de leite
leite condensado
```

Contagens por classificação:

```text
valid_candidate: 3
suspicious_candidate: 0
invalid_candidate: 0
```

## 8. Imutabilidade e diff semântico

O `CatalogImportReviewedResolutionLoader` e o `CatalogImportReviewedResolutionValidator` aceitaram o artefato rastreável usando o template aprovado como baseline.

Resultados da comparação adicional:

```text
entry order preserved: 106/106
unchanged entries outside approved set: 103/103
selected entries with all immutable facts equal: 3/3
total entries with immutable facts preserved: 106/106
candidate manifest binding unchanged: yes
preflight evidence unchanged: yes
planned reference UUID evidence unchanged: yes
alias display/raw variants/ordinals unchanged: yes
```

Para cada entrada selecionada, somente estes campos tiveram bytes diferentes do template:

```text
alias_decisions.*.alias_kind
alias_decisions.*.status
catalog_classification
editorial_notes
is_generic
owner_user_id_decision
preparation_decision.status
reference_target
reference_visibility
selected_for_apply
stable_key
version_locale
```

Além das entradas, somente o summary editorial mudou para as contagens derivadas aprovadas. Nenhum fato nutricional, source claim, confidence claim, canonical name, alias fact, issue code, collision, provenance ou UUID planejado foi corrigido ou recalculado.

## 9. Determinismo e integridade do artefato

Duas execuções independentes partiram do mesmo template e do mesmo conjunto de decisões.

```text
run 1 SHA-256: 8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d
run 2 SHA-256: 8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d
run 1 bytes: 230948
run 2 bytes: 230948
cmp result: byte-identical
summary comparison: identical
selected candidate order: identical
alias decision order: identical
```

A cópia durável foi feita dos bytes exatos da primeira execução, sem decode/re-encode. Depois da cópia:

```text
durable byte size: 230948
durable SHA-256: 8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d
filename checksum match: yes
temporary-to-durable cmp: byte-identical
canonical JSON round trip: byte-identical
```

O artefato não inclui timestamp, reviewer identity, approval reason, actor, absolute path, PID ou UUID aleatória.

## 10. Testes executados

Comando obrigatório:

```bash
./vendor/bin/sail artisan test --compact \
  tests/Unit/Nutrition/Catalog/Import/CatalogImportReviewedResolutionTest.php \
  tests/Unit/Nutrition/Catalog/Import/CatalogImportReviewPacketTest.php \
  tests/Unit/Nutrition/Catalog/Import/CatalogImportReviewBoundaryTest.php
```

Resultado:

```text
38 passed
1953 assertions
duration: 1.81s
exit code: 0
```

Testes adicionais existentes de integridade dos inputs:

```bash
./vendor/bin/sail artisan test --compact \
  tests/Unit/Nutrition/Catalog/Import/ApprovedCandidateManifestArtifactTest.php \
  tests/Unit/Nutrition/Catalog/Import/ApprovedResolutionTemplateArtifactTest.php
```

Resultado:

```text
2 passed
17 assertions
duration: 0.14s
exit code: 0
```

Total desta fase:

```text
40 tests passed
1970 assertions
```

Nenhum teste foi criado, editado ou removido. Pint não foi necessário porque nenhum arquivo PHP rastreado foi alterado; o script temporário já havia sido removido.

## 11. Comandos operacionais principais

### 11.1 Preflight

```bash
./vendor/bin/sail artisan catalog:prepare-legacy-import-review \
  --manifest=resources/catalog-import/approved/legacy_config_nutrition_v1/candidate-manifest-4e5e5c3c505fca1d613ef8c3dee6bd066cd28876a49cd1b47dd543d9b4996ee2.json \
  --expected-manifest-sha256=4e5e5c3c505fca1d613ef8c3dee6bd066cd28876a49cd1b47dd543d9b4996ee2 \
  --resolution-output=storage/app/m244d-current-unresolved-template.json \
  --preflight-report=storage/app/m244d-current-preflight.json
```

### 11.2 Geração independente

```bash
./vendor/bin/sail php storage/app/m244d-generate-reviewed-resolution.php storage/app/m244d-reviewed-resolution-run-1.json
./vendor/bin/sail php storage/app/m244d-generate-reviewed-resolution.php storage/app/m244d-reviewed-resolution-run-2.json
```

### 11.3 Comparação e preservação exata

```bash
cmp -s storage/app/m244d-reviewed-resolution-run-1.json storage/app/m244d-reviewed-resolution-run-2.json
sha256sum storage/app/m244d-reviewed-resolution-run-1.json storage/app/m244d-reviewed-resolution-run-2.json
wc -c storage/app/m244d-reviewed-resolution-run-1.json storage/app/m244d-reviewed-resolution-run-2.json
cp --no-clobber storage/app/m244d-reviewed-resolution-run-1.json resources/catalog-import/review/legacy_config_nutrition_v1/reviewed-resolution-8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d.json
cmp -s storage/app/m244d-reviewed-resolution-run-1.json resources/catalog-import/review/legacy_config_nutrition_v1/reviewed-resolution-8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d.json
```

### 11.4 Validações de estado

Foram executados ainda:

```bash
sha256sum <manifest> <unresolved-template> <reviewed-resolution>
wc -c <reviewed-resolution>
git diff --exit-code HEAD -- <manifest> <unresolved-template>
git check-ignore -v storage/app/m244d-current-unresolved-template.json storage/app/m244d-current-preflight.json
git status --short
git diff --check
```

As inspeções JSON e validações adicionais foram executadas com `./vendor/bin/sail php -r '...'`, usando os loaders, serializer e validators comprometidos. Nenhuma escrita de banco ou Tinker foi usada.

O comando abaixo não foi executado:

```text
catalog:build-legacy-import-apply-plan
```

## 12. Segurança e exclusões confirmadas

- Nenhum documento `nutria.catalog-import-resolution-approval/1` foi criado.
- Nenhum reviewer reference, approval reason ou approved-at timestamp foi inventado.
- Nenhum apply plan real ou diagnóstico foi criado nesta fase.
- Nenhum comando de build de apply plan foi chamado.
- Nenhum registro de banco foi criado ou modificado pelo preflight/geração/validação.
- Nenhuma migration, seeder ou write command foi executado.
- Nenhum lifecycle ou supersession service foi invocado.
- Nenhum production code, teste, migration, model, factory, constraint, meal, resolver, estimate ou valor nutricional foi alterado.
- Nenhuma dependência foi adicionada.
- Nenhuma fonte externa, HTTP, AI, LLM, RAG, embedding, memória ou histórico foi usada como evidência.
- Valores de 313, 306 e 221 kcal por 100 g permanecem claims legados `primary/untrusted`, sem verificação nutricional.
- Stable keys permanecem decisões humanas específicas deste lote e não estabelecem convenção global.
- Versions e aliases eventualmente criados em fase futura permanecerão drafts.

## 13. Arquivos temporários e diagnósticos

Removidos após o uso:

```text
storage/app/m244d-generate-reviewed-resolution.php
storage/app/m244d-reviewed-resolution-run-1.json
storage/app/m244d-reviewed-resolution-run-2.json
```

Mantidos como diagnósticos ignorados e não rastreados:

```text
storage/app/m244d-current-unresolved-template.json
storage/app/m244d-current-preflight.json
```

Ambos são cobertos por:

```text
storage/app/.gitignore:1:*
```

## 14. Validação final

Resultados efetivamente observados após a criação deste relatório:

```text
git diff --check: exit 0, sem saída
supplemental report trailing-whitespace scan: exit 0, sem saída
manifest/template diff against HEAD: exit 0, sem saída
manifest SHA-256: 4e5e5c3c505fca1d613ef8c3dee6bd066cd28876a49cd1b47dd543d9b4996ee2
unresolved template SHA-256: b9c1d4ae30c70208bf57bea51e6a6824886e129ecda20afe632ea3f47d28889e
reviewed-resolution SHA-256: 8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d
reviewed-resolution bytes: 230948
reviewed-resolution filename checksum: match
reviewed-resolution canonical round trip: byte-identical
temporary generator and run outputs: absent
diagnostic outputs: ignored by storage/app/.gitignore:1:*
```

O `git status --short` observado é exatamente:

```text
?? M2.4.4d-CANONICAL-REVIEWED-RESOLUTION-FIRST-BATCH-REPORT.md
?? resources/catalog-import/review/legacy_config_nutrition_v1/reviewed-resolution-8eb9db29c044712134c4597220bdb7e61b19f186395c3dcc289cfe31c0054a5d.json
```

Esses são os únicos dois additions rastreáveis pretendidos. Nenhum commit foi criado automaticamente.
